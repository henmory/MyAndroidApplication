1.动态代理设计模式，要求我们不用写代理类

2.但是需要的文件包括：
    1.实体类
    2.接口类
    3.StarHandler  处理类＝＝＝实际处理的类实现InvocationHandler接口
    4.在客户端生成一个star代理对象采用（Proxy类实现）

3.该实例表明，动态代理不需要我们自己写代理类，而是在用的时候动态的用proxy生成一个代理的对象，通过该代理对象操作接口中的方法

4.该方法最终要的是jdk给我们提供的两个类

    1.proxy：动态生成代理类
    2.InvocationHandler接口：通过它的invoke实现对真是角色对访问
