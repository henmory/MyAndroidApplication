1.迭代器设计模式是为了迭代集合的

2.因而对于不同的集合使用的迭代器是不一样的，进而我们的迭代器最好作为一个集合私有的内部的类，因为该迭代器类放到外面是不能用的

3.所有，首先有个Iterator迭代器抽象类，有next 和 hasNext接口

4.有个抽象的Aggragate作为抽象的聚合类， 里面有添加删除和创建迭代器接口

5.具体的ConcreteAggregate，实现抽象聚合类，并写一个私有的迭代器类，在创建迭代器的时候，直接返回这个私有类的对象

6.注意迭代器创建返回的类型要用抽象的Iterator

7.在具体的迭代器内部，需要有个cursor变量记录遍历的位置，默认为0


8.jdk中的迭代器就是这样实现的，Arraylist也是用内部类的形式定义了迭代器， Iterator就是哪个抽象的迭代器接口