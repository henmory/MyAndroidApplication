1.外观模式是最简单的设计模式

2.注意是自己写的东西或者调用别人写的东西的时候，调用简单

3.因此需要我们自己封装，然后client调用的时候，直接调用最简单的接口即可

4.它体现了一个设计模式的原则：最少知识原则，即我们调用另一个类的时候，尽可能知道另一个类少些信息

5.这样我们就只调用一个简单的接口即可

6.没有写代码是因为平时写代码的时候都在用，简单可以理解为自己封装

7.外观模式的核心是，要求一个子系统外部与内部的通信，通过一个统一的对象进行，该对象封装统一的接口